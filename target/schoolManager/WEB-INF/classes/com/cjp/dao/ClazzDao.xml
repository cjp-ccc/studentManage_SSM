<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
		PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cjp.dao.ClazzDao">

	<select id="findList" parameterType="Map" resultType="Clazz">
		select * from clazz where 1 = 1
		<if test="name != null">
			and name like #{name}
		</if>
		<if test="professionId != null">
			and profession_id = #{professionId}
		</if>
		limit #{offset},#{pageSize}
	</select>

	<select id="findAll" resultType="Clazz">
		select * from clazz
	</select>

	<select id="getTotal" parameterType="Map" resultType="Integer">
		select count(id) from clazz where 1 = 1
		<if test="name != null">
			and name like #{name}
		</if>

		<if test="professionId != null">
			and profession_id = #{professionId}
		</if>
	</select>

	<update id="add" parameterType="Clazz">
		insert into clazz(profession_id,name) values(#{professionId},#{name})
	</update>

	<update id="edit" parameterType="Clazz">
		update clazz set profession_id = #{professionId},name = #{name} where id = #{id}
	</update>

	<update id="delete" parameterType="String">
		delete from clazz where id in (${value})
	</update>
</mapper>