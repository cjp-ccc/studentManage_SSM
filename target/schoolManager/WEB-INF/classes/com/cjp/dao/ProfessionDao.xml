<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
		PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cjp.dao.ProfessionDao">

	<select id="findList" parameterType="Map" resultType="Profession">
		select * from profession where 1 = 1
		<if test="name != null">
			and name like #{name}
		</if>
		<if test="academyId != null">
			and academy_id = #{academyId}
		</if>
		limit #{offset},#{pageSize}
	</select>

	<select id="findAll" resultType="Profession">
		select * from profession
	</select>

	<select id="getTotal" parameterType="Map" resultType="Integer">
		select count(id) from profession where 1 = 1
		<if test="name != null">
			and name like #{name}
		</if>

		<if test="academyId != null">
			and academy_id = #{academyId}
		</if>
	</select>

	<update id="add" parameterType="Profession">
		insert into profession(academy_id,name) values(#{academyId},#{name})
	</update>

	<update id="edit" parameterType="Profession">
		update profession set academy_id = #{academyId},name = #{name} where id = #{id}
	</update>

	<update id="delete" parameterType="String">
		delete from profession where id in (${value})
	</update>
</mapper>